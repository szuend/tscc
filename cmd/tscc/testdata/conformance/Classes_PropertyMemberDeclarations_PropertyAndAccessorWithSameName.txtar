# Ported from tests/cases/conformance/classes/propertyMemberDeclarations/propertyAndAccessorWithSameName.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js propertyAndAccessorWithSameName.js propertyAndAccessorWithSameName.ts
stderr 'TS2300'
! exists propertyAndAccessorWithSameName.js
! exists propertyAndAccessorWithSameName.d.ts
! exists propertyAndAccessorWithSameName.js.map

-- propertyAndAccessorWithSameName.ts --
class C {
    x: number;
    get x() { // error
        return 1;
    }
}

class D {
    x: number;
    set x(v) { } // error
}

class E {
    private x: number;
    get x() { // error
        return 1;
    }
    set x(v) { }
}
