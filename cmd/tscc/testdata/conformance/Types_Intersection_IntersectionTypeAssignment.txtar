# Ported from tests/cases/conformance/types/intersection/intersectionTypeAssignment.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js intersectionTypeAssignment.js intersectionTypeAssignment.ts
stderr 'TS2741'
stderr 'TS2322'
! exists intersectionTypeAssignment.js
! exists intersectionTypeAssignment.d.ts
! exists intersectionTypeAssignment.js.map

-- intersectionTypeAssignment.ts --
declare var a: { a: string };
declare var b: { b: string };
declare var x: { a: string, b: string };
declare var y: { a: string } & { b: string };

a = x;
a = y;
x = a;  // Error
y = a;  // Error

b = x;
b = y;
x = b;  // Error
y = b;  // Error

x = y;
y = x;
