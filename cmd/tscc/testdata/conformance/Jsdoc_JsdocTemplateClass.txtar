# Ported from tests/cases/conformance/jsdoc/jsdocTemplateClass.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --allow-js --check-js --target es2015 --lib es2015,dom templateTagOnClasses.js
stderr 'TS2304'
stderr 'TS2322'
! exists templateTagOnClasses.d.ts
! exists templateTagOnClasses.js.map

-- templateTagOnClasses.js --
/**
 * @template T
 * @typedef {(t: T) => T} Id
 */
/** @template T */
class Foo {
    /** @typedef {(t: T) => T} Id2 */
    /** @param {T} x */
    constructor (x) {
        this.a = x
    }
    /**
     * 
     * @param {T} x 
     * @param {Id<T>} y
     * @param {Id2} alpha
     * @return {T}
     */
    foo(x, y, alpha) {
        return alpha(y(x))
    }
}
var f = new Foo(1)
var g = new Foo(false)
f.a = g.a
