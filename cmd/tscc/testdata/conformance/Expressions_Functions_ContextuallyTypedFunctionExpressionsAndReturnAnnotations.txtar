# Ported from tests/cases/conformance/expressions/functions/contextuallyTypedFunctionExpressionsAndReturnAnnotations.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --no-strict --target es2015 --lib es2015,dom --out-js contextuallyTypedFunctionExpressionsAndReturnAnnotations.js contextuallyTypedFunctionExpressionsAndReturnAnnotations.ts
! stderr .
cmp contextuallyTypedFunctionExpressionsAndReturnAnnotations.js contextuallyTypedFunctionExpressionsAndReturnAnnotations.js.golden
! exists contextuallyTypedFunctionExpressionsAndReturnAnnotations.d.ts
! exists contextuallyTypedFunctionExpressionsAndReturnAnnotations.js.map

-- contextuallyTypedFunctionExpressionsAndReturnAnnotations.ts --
declare function foo(x: (y: string) => (y2: number) => void);

// Contextually type the parameter even if there is a return annotation
foo((y): (y2: number) => void => {
    var z = y.charAt(0); // Should be string
    return null;
});

foo((y: string) => {
    return y2 => {
        var z = y2.toFixed(); // Should be string
        return 0;
    };
});
-- contextuallyTypedFunctionExpressionsAndReturnAnnotations.js.golden --
"use strict";
// Contextually type the parameter even if there is a return annotation
foo((y) => {
    var z = y.charAt(0); // Should be string
    return null;
});
foo((y) => {
    return y2 => {
        var z = y2.toFixed(); // Should be string
        return 0;
    };
});
