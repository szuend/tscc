# Ported from tests/cases/conformance/types/objectTypeLiteral/callSignatures/specializedSignatureIsNotSubtypeOfNonSpecializedSignature.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --out-dts specializedSignatureIsNotSubtypeOfNonSpecializedSignature.d.ts --no-strict --target es2015 --lib es2015,dom --out-js specializedSignatureIsNotSubtypeOfNonSpecializedSignature.js specializedSignatureIsNotSubtypeOfNonSpecializedSignature.ts
stderr 'TS2394'
! exists specializedSignatureIsNotSubtypeOfNonSpecializedSignature.d.ts
! exists specializedSignatureIsNotSubtypeOfNonSpecializedSignature.js
! exists specializedSignatureIsNotSubtypeOfNonSpecializedSignature.js.map

-- specializedSignatureIsNotSubtypeOfNonSpecializedSignature.ts --
function foo(x: 'a');
function foo(x: number) { }

class C {
    foo(x: 'a');
    foo(x: number);
    foo(x: any) { }
}

class C2<T> {
    foo(x: 'a');
    foo(x: T);
    foo(x: any) { }
}

class C3<T extends String> {
    foo(x: 'a');
    foo(x: T);
    foo(x: any) { }
}

interface I {
    (x: 'a');
    (x: number);
    foo(x: 'a');
    foo(x: number);
}

interface I2<T> {
    (x: 'a');
    (x: T);
    foo(x: 'a');
    foo(x: T);
}

interface I3<T extends String> {
    (x: 'a');
    (x: T);
    foo(x: 'a');
    foo(x: T);
}

var a: {
    (x: 'a');
    (x: number);
    foo(x: 'a');
    foo(x: number);
}

var a2: {
    (x: 'a');
    <T>(x: T);
    foo(x: 'a');
    foo<T>(x: T);
}

var a3: {
    (x: 'a');
    <T>(x: T);
    foo(x: 'a');
    foo<T extends String>(x: T);
}
