# Ported from tests/cases/conformance/jsdoc/paramTagWrapping.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --allow-js --check-js --strict --target es2015 --lib es2015,dom good.js
! stderr .
! exists good.d.ts
! exists good.js.map

-- bad.js --
/**
 * @param *
 * {number} x Arg x.
 * @param {number}
 * * y Arg y.
 * @param {number} * z
 * Arg z.
 */
function bad(x, y, z) {
}

bad(1, 2, 3)
-- good.js --
/**
 * @param
 * {number} x Arg x.
 * @param {number}
 * y Arg y.
 * @param {number} z
 * Arg z.
 */
function good(x, y, z) {
}

good(1, 2, 3)

