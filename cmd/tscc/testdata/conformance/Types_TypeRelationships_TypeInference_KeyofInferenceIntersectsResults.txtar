# Ported from tests/cases/conformance/types/typeRelationships/typeInference/keyofInferenceIntersectsResults.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js keyofInferenceIntersectsResults.js keyofInferenceIntersectsResults.ts
! stderr .
cmp keyofInferenceIntersectsResults.js keyofInferenceIntersectsResults.js.golden
! exists keyofInferenceIntersectsResults.d.ts
! exists keyofInferenceIntersectsResults.js.map

-- keyofInferenceIntersectsResults.ts --
interface X {
    a: string;
    b: string;
}

declare function foo<T = X>(x: keyof T, y: keyof T): T;
declare function bar<T>(x: keyof T, y: keyof T): T;

const a = foo<X>('a', 'b'); // compiles cleanly
const b = foo('a', 'b');    // also clean
const c = bar('a', 'b');    // still clean
-- keyofInferenceIntersectsResults.js.golden --
"use strict";
const a = foo('a', 'b'); // compiles cleanly
const b = foo('a', 'b'); // also clean
const c = bar('a', 'b'); // still clean
