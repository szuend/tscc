# Ported from tests/cases/conformance/jsdoc/callbackTag2.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --allow-js --check-js --no-strict --target es2015 --lib es2015,dom cb.js
stderr 'TS2339'
! exists cb.d.ts
! exists cb.js.map

-- cb.js --
/** @template T
 * @callback Id
 * @param {T} t
 * @returns {T} Maybe just return 120 and cast it?
 */
var x = 1

/** @type {Id<string>} I actually wanted to write `const "120"` */
var one_twenty = s => "120";

/** @template S
 * @callback SharedId
 * @param {S} ego
 * @return {S}
 */
class SharedClass {
    constructor() {
        /** @type {SharedId<S>} */
        this.id;
    }
}
/** @type {SharedId<number>} */
var outside = n => n + 1;

/** @type {Final<{ fantasy }, { heroes }>} */
var noreturn = (barts, tidus, noctis) => "cecil"

/**
 * @template V,X
 * @callback Final
 * @param {V} barts - "Barts"
 * @param {X} tidus - Titus
 * @param {X & V} noctis - "Prince Noctis Lucius Caelum"
 * @return {"cecil" | "zidane"}
 */
