# Ported from tests/cases/conformance/internalModules/importDeclarations/invalidImportAliasIdentifiers.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js invalidImportAliasIdentifiers.js invalidImportAliasIdentifiers.ts
stderr 'TS2503'
stderr 'TS2564'
stderr 'TS2702'
! exists invalidImportAliasIdentifiers.js
! exists invalidImportAliasIdentifiers.d.ts
! exists invalidImportAliasIdentifiers.js.map

-- invalidImportAliasIdentifiers.ts --
// none of these should work, since non are actually modules

var V = 12;

import v = V;

class C {
    name: string;
}

import c = C;

enum E {
    Red, Blue
}

import e = E;

interface I {
    id: number;
}

import i = I;
