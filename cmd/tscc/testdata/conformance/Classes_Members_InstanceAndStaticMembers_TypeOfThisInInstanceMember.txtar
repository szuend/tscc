# Ported from tests/cases/conformance/classes/members/instanceAndStaticMembers/typeOfThisInInstanceMember.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js typeOfThisInInstanceMember.js typeOfThisInInstanceMember.ts
stderr 'TS2339'
! exists typeOfThisInInstanceMember.js
! exists typeOfThisInInstanceMember.d.ts
! exists typeOfThisInInstanceMember.js.map

-- typeOfThisInInstanceMember.ts --
class C {
    x = this;
    foo() {
        return this;
    }
    constructor(x: number) {
        var t = this;
        t.x;
        t.y;
        t.z;
        var r = t.foo();
    }

    get y() {
        return this;
    }
}

declare var c: C;
// all ok
var r = c.x;
var ra = c.x.x.x;
var r2 = c.y;
var r3 = c.foo();
var rs = [r, r2, r3];

rs.forEach(x => {
    x.foo;
    x.x;
    x.y;
});
