# Ported from tests/cases/conformance/types/contextualTypes/jsdoc/contextualTypeFromJSDoc.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --allow-js --check-js --target esnext --lib esnext,dom index.js
! stderr .
! exists index.d.ts
! exists index.js.map

-- index.js --
/** @type {Array<[string, {x?:number, y?:number}]>} */
const arr = [
    ['a', { x: 1 }],
    ['b', { y: 2 }]
];

/** @return {Array<[string, {x?:number, y?:number}]>} */
function f() {
    return [
        ['a', { x: 1 }],
        ['b', { y: 2 }]
    ];
}

class C {
    /** @param {Array<[string, {x?:number, y?:number}]>} value */
    set x(value) { }
    get x() {
        return [
            ['a', { x: 1 }],
            ['b', { y: 2 }]
        ];
    }
}
