# Ported from tests/cases/conformance/expressions/asOperator/asOpEmitParens.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --no-strict --target es2015 --lib es2015,dom --out-js asOpEmitParens.js asOpEmitParens.ts
! stderr .
cmp asOpEmitParens.js asOpEmitParens.js.golden
! exists asOpEmitParens.d.ts
! exists asOpEmitParens.js.map

-- asOpEmitParens.ts --
declare var x;
// Must emit as (x + 1) * 3
(x + 1 as number) * 3;

// Should still emit as x.y
(x as any).y;

// Emit as new (x())
new (x() as any);
-- asOpEmitParens.js.golden --
"use strict";
// Must emit as (x + 1) * 3
(x + 1) * 3;
// Should still emit as x.y
x.y;
// Emit as new (x())
new (x());
