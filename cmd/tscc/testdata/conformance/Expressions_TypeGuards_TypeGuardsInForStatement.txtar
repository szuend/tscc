# Ported from tests/cases/conformance/expressions/typeGuards/typeGuardsInForStatement.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js typeGuardsInForStatement.js typeGuardsInForStatement.ts
stderr 'TS2322'
! exists typeGuardsInForStatement.js
! exists typeGuardsInForStatement.d.ts
! exists typeGuardsInForStatement.js.map

-- typeGuardsInForStatement.ts --
let cond: boolean;
function a(x: string | number) {
    for (x = undefined; typeof x !== "number"; x = undefined) {
        x; // string
    }
    x; // number
}
function b(x: string | number) {
    for (x = undefined; typeof x !== "number"; x = undefined) {
        x; // string
        if (cond) continue;
    }
    x; // number
}
function c(x: string | number) {
    for (x = undefined; typeof x !== "number"; x = undefined) {
        x; // string
        if (cond) break;
    }
    x; // string | number
}
