# Ported from tests/cases/conformance/types/typeRelationships/typeInference/noInferRedeclaration.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --out-dts a.d.ts --strict --target esnext --lib esnext,dom --out-js a.js a.ts
! stderr .
cmp a.d.ts a.d.ts.golden
cmp a.js a.js.golden
! exists a.js.map

-- a.ts --
export const f = <T>(x: T, y: NoInfer<T>) => x;
-- b.ts --
import { f } from "./a";

type NoInfer<T> = T & number;

export const g = f;
-- a.d.ts.golden --
export declare const f: <T>(x: T, y: NoInfer<T>) => T;
-- a.js.golden --
export const f = (x, y) => x;
