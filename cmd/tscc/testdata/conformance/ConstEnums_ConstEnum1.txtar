# Ported from tests/cases/conformance/constEnums/constEnum1.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --out-dts constEnum1.d.ts --target es2015 --lib es2015,dom --out-js constEnum1.js constEnum1.ts
! stderr .
cmp constEnum1.d.ts constEnum1.d.ts.golden
cmp constEnum1.js constEnum1.js.golden
! exists constEnum1.js.map

-- constEnum1.ts --
// An enum declaration that specifies a const modifier is a constant enum declaration.
// In a constant enum declaration, all members must have constant values and
// it is an error for a member declaration to specify an expression that isn't classified as a constant enum expression.

const enum E {
    a = 10,
    b = a,
    c = (a+1),
    e,
    d = ~e,
    f = a << 2 >> 1,
    g = a << 2 >>> 1,
    h = a | b
}
-- constEnum1.d.ts.golden --
declare const enum E {
    a = 10,
    b = 10,
    c = 11,
    e = 12,
    d = -13,
    f = 20,
    g = 20,
    h = 10
}
-- constEnum1.js.golden --
"use strict";
// An enum declaration that specifies a const modifier is a constant enum declaration.
// In a constant enum declaration, all members must have constant values and
// it is an error for a member declaration to specify an expression that isn't classified as a constant enum expression.
