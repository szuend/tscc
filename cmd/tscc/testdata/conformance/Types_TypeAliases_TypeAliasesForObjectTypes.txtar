# Ported from tests/cases/conformance/types/typeAliases/typeAliasesForObjectTypes.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js typeAliasesForObjectTypes.js typeAliasesForObjectTypes.ts
stderr 'TS2564'
stderr 'TS2300'
! exists typeAliasesForObjectTypes.js
! exists typeAliasesForObjectTypes.d.ts
! exists typeAliasesForObjectTypes.js.map

-- typeAliasesForObjectTypes.ts --
type T1 = { x: string }

// An interface can be named in an extends or implements clause, but a type alias for an object type literal cannot.
interface I1 extends T1 { y: string }
class C1 implements T1 {
    x: string;
}

// An interface can have multiple merged declarations, but a type alias for an object type literal cannot.
type T2 = { x: string }
type T2 = { y: number }

// An interface can have type parameters, but a type alias for an object type literal cannot.
type T3<T> = { x: T }
