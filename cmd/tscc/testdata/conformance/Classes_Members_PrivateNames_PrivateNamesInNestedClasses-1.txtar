# Ported from tests/cases/conformance/classes/members/privateNames/privateNamesInNestedClasses-1.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --strict --target es6 --lib es6,dom --out-js privateNamesInNestedClasses-1.js privateNamesInNestedClasses-1.ts
stderr 'TS18014'
! exists privateNamesInNestedClasses-1.js
! exists privateNamesInNestedClasses-1.d.ts
! exists privateNamesInNestedClasses-1.js.map

-- privateNamesInNestedClasses-1.ts --
class A {
   #foo = "A's #foo";
   #bar = "A's #bar";
   method () {
       class B {
           #foo = "B's #foo";
           bar (a: any) {
               a.#foo; // OK, no compile-time error, don't know what `a` is
           }
           baz (a: A) {
               a.#foo; // compile-time error, shadowed
           }
           quux (b: B) {
               b.#foo; // OK
           }
       }
       const a = new A();
       new B().bar(a);
       new B().baz(a);
       const b = new B();
       new B().quux(b);
   }
}

new A().method();
