# Ported from tests/cases/conformance/types/typeRelationships/assignmentCompatibility/typeParameterAssignability3.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js typeParameterAssignability3.js typeParameterAssignability3.ts
stderr 'TS2564'
stderr 'TS2454'
stderr 'TS2322'
! exists typeParameterAssignability3.js
! exists typeParameterAssignability3.d.ts
! exists typeParameterAssignability3.js.map

-- typeParameterAssignability3.ts --
// type parameters are not assignable to one another unless directly or indirectly constrained to one another

class Foo { foo: string; }

function foo<T extends Foo, U extends Foo>(t: T, u: U) {
    var a: T;
    var b: U;
    t = a; // ok
    a = t; // ok

    b = u; // ok
    u = b; // ok

    t = u; // error
    u = t; // error
}

class C<T extends Foo, U extends Foo> {
    t: T;
    u: U;
    r = () => {
        this.t = this.u; // error
        this.u = this.t; // error
    }
}
