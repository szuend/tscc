# Ported from tests/cases/conformance/types/primitives/number/invalidNumberAssignments.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js invalidNumberAssignments.js invalidNumberAssignments.ts
stderr 'TS2322'
stderr 'TS2564'
stderr 'TS2631'
stderr 'TS2630'
! exists invalidNumberAssignments.js
! exists invalidNumberAssignments.d.ts
! exists invalidNumberAssignments.js.map

-- invalidNumberAssignments.ts --
var x = 1;

var a: boolean = x;
var b: string = x;
var c: void = x;
var d: typeof undefined = x;

class C { foo: string; }
var e: C = x;

interface I { bar: string; }
var f: I = x;

var g: { baz: string } = 1;
var g2: { 0: number } = 1;

namespace M { export var x = 1; }
M = x;

function i<T>(a: T) {
    a = x;
}
i = x;
