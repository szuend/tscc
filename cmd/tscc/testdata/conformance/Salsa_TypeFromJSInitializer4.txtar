# Ported from tests/cases/conformance/salsa/typeFromJSInitializer4.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --allow-js --check-js --no-implicit-any --no-strict-null-checks --target es2015 --lib es2015,dom a.js
stderr 'TS7006'
stderr 'TS2322'
! exists a.d.ts
! exists a.js.map

-- a.js --
/** @type {number | undefined} */
var n;

// should get any on parameter initialisers
function f(a = null, b = n, l = []) {
    // a should be any
    a = undefined
    a = null
    a = 1
    a = true
    a = {}
    a = 'ok'

    // b should be number | undefined, not any
    b = 1
    b = undefined
    b = 'error'

    // l should be any[]
    l.push(1)
    l.push('ok')
}
