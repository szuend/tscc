# Ported from tests/cases/conformance/types/nonPrimitive/nonPrimitiveNarrow.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js nonPrimitiveNarrow.js nonPrimitiveNarrow.ts
stderr 'TS18047'
! exists nonPrimitiveNarrow.js
! exists nonPrimitiveNarrow.d.ts
! exists nonPrimitiveNarrow.js.map

-- nonPrimitiveNarrow.ts --
class Narrow {
    narrowed!: boolean
}

declare var a: object;

if (a instanceof Narrow) {
    a.narrowed; // ok
    a = 123; // error
}

if (typeof a === 'number') {
    a.toFixed(); // error, never
}

declare var b: object | null;

if (typeof b === 'object') {
   b.toString(); // ok, object | null
} else {
   b.toString(); // error, never
}
