# Ported from tests/cases/conformance/expressions/typeGuards/typeGuardsInDoStatement.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js typeGuardsInDoStatement.js typeGuardsInDoStatement.ts
stderr 'TS2322'
! exists typeGuardsInDoStatement.js
! exists typeGuardsInDoStatement.d.ts
! exists typeGuardsInDoStatement.js.map

-- typeGuardsInDoStatement.ts --
let cond: boolean;
function a(x: string | number | boolean) {
    x = true;
    do {
        x; // boolean | string
        x = undefined;
    } while (typeof x === "string")
    x; // number | boolean
}
function b(x: string | number | boolean) {
    x = true;
    do {
        x; // boolean | string
        if (cond) continue;
        x = undefined;
    } while (typeof x === "string")
    x; // number | boolean
}
function c(x: string | number) {
    x = "";
    do {
        x; // string
        if (cond) break;
        x = undefined;
    } while (typeof x === "string")
    x; // string | number
}
