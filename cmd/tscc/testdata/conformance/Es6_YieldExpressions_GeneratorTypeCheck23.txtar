# Ported from tests/cases/conformance/es6/yieldExpressions/generatorTypeCheck23.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target ES6 --lib ES6,dom --out-js generatorTypeCheck23.js generatorTypeCheck23.ts
stderr 'TS2564'
! exists generatorTypeCheck23.js
! exists generatorTypeCheck23.d.ts
! exists generatorTypeCheck23.js.map

-- generatorTypeCheck23.ts --
class Foo { x: number }
class Bar extends Foo { y: string }
class Baz { z: number }
function* g3() {
    yield;
    yield new Foo;
    yield new Bar;
    yield new Baz;
    yield *[new Bar];
    yield *[new Baz];
}
