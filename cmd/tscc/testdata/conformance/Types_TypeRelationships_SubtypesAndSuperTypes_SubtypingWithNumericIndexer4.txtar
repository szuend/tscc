# Ported from tests/cases/conformance/types/typeRelationships/subtypesAndSuperTypes/subtypingWithNumericIndexer4.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js subtypingWithNumericIndexer4.js subtypingWithNumericIndexer4.ts
stderr 'TS2415'
stderr 'TS2741'
! exists subtypingWithNumericIndexer4.js
! exists subtypingWithNumericIndexer4.d.ts
! exists subtypingWithNumericIndexer4.js.map

-- subtypingWithNumericIndexer4.ts --
// Derived type indexer must be subtype of base type indexer

interface Base { foo: string; }
interface Derived extends Base { bar: string; }
interface Derived2 extends Derived { baz: string; }

class A {
    [x: number]: Derived;
}

class B extends A {
    [x: number]: string; // error
}

namespace Generics {
    class A<T extends Derived> {
        [x: number]: T;
    }

    class B extends A<Base> {
        [x: number]: string; // error
    }

    class B3<T extends Derived> extends A<T> {
        [x: number]: string; // error
    }
}
