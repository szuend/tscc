# Ported from tests/cases/conformance/types/forAwait/types.forAwait.es2018.2.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --lib esnext --target es2018 types.forAwait.es2018.2.ts
stderr 'TS2504'
stderr 'TS2322'
stderr 'TS2488'
! exists types.forAwait.es2018.2.d.ts
! exists types.forAwait.es2018.2.js.map

-- types.forAwait.es2018.2.ts --
declare const asyncIterable: AsyncIterable<number>;
declare const iterable: Iterable<number>;
async function f() {
    let y: number;
    let z: string;
    for await (const x of {}) {
    }
    for await (y of {}) {
    }
    for await (z of asyncIterable) {
    }
    for await (z of iterable) {
    }
    for (const x of asyncIterable) {
    }
    for (y of asyncIterable) {
    }
}
