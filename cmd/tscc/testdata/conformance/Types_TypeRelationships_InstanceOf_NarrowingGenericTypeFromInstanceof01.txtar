# Ported from tests/cases/conformance/types/typeRelationships/instanceOf/narrowingGenericTypeFromInstanceof01.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js narrowingGenericTypeFromInstanceof01.js narrowingGenericTypeFromInstanceof01.ts
stderr 'TS2741'
! exists narrowingGenericTypeFromInstanceof01.js
! exists narrowingGenericTypeFromInstanceof01.d.ts
! exists narrowingGenericTypeFromInstanceof01.js.map

-- narrowingGenericTypeFromInstanceof01.ts --
class A<T> {
    constructor(private a: string) { }
}

class B<T> {
}

function acceptA<T>(a: A<T>) { }
function acceptB<T>(b: B<T>) { }

function test<T>(x: A<T> | B<T>) {
    if (x instanceof B) {
        acceptA(x);
    }

    if (x instanceof A) {
        acceptA(x);
    }

    if (x instanceof B) {
        acceptB(x);
    }

    if (x instanceof B) {
        acceptB(x);
    }
}
