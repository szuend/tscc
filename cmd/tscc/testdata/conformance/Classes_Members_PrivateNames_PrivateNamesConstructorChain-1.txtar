# Ported from tests/cases/conformance/classes/members/privateNames/privateNamesConstructorChain-1.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js privateNamesConstructorChain-1.js privateNamesConstructorChain-1.ts
stderr 'TS2339'
! exists privateNamesConstructorChain-1.js
! exists privateNamesConstructorChain-1.d.ts
! exists privateNamesConstructorChain-1.js.map

-- privateNamesConstructorChain-1.ts --
class Parent {
    #foo = 3;
    static #bar = 5;
    accessChildProps() {
        new Child().#foo; // OK (`#foo` was added when `Parent`'s constructor was called on `child`)
        Child.#bar;       // Error: not found
    }
}

class Child extends Parent {
    #foo = "foo";       // OK (Child's #foo does not conflict, as `Parent`'s `#foo` is not accessible)
    #bar = "bar";       // OK
}
