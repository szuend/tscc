# Ported from tests/cases/conformance/salsa/typeFromParamTagForFunction.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --allow-js --check-js --module commonjs --target es2015 --lib es2015,dom e.js
! stderr .
! exists e.d.ts
! exists e.js.map

-- a-ext.js --
exports.A = function () {
    this.x = 1;
};
-- a.js --
const { A } = require("./a-ext");

/** @param {A} p */
function a(p) { p.x; }
-- b-ext.js --
exports.B = class {
    constructor() {
        this.x = 1;
    }
};
-- b.js --
const { B } = require("./b-ext");

/** @param {B} p */
function b(p) { p.x; }
-- c-ext.js --
export function C() {
    this.x = 1;
}
-- c.js --
const { C } = require("./c-ext");

/** @param {C} p */
function c(p) { p.x; }
-- d-ext.js --
export var D = function() {
    this.x = 1;
};
-- d.js --
const { D } = require("./d-ext");

/** @param {D} p */
function d(p) { p.x; }
-- e-ext.js --
export class E {
    constructor() {
        this.x = 1;
    }
}
-- e.js --
const { E } = require("./e-ext");

/** @param {E} p */
function e(p) { p.x; }
-- f.js --
var F = function () {
    this.x = 1;
};

/** @param {F} p */
function f(p) { p.x; }
-- g.js --
function G() {
    this.x = 1;
}

/** @param {G} p */
function g(p) { p.x; }
-- h.js --
class H {
    constructor() {
        this.x = 1;
    }
}

/** @param {H} p */
function h(p) { p.x; }
-- node.d.ts --
declare function require(id: string): any;
declare var module: any, exports: any;
