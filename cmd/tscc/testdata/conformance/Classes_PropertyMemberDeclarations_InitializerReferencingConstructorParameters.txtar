# Ported from tests/cases/conformance/classes/propertyMemberDeclarations/initializerReferencingConstructorParameters.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js initializerReferencingConstructorParameters.js initializerReferencingConstructorParameters.ts
stderr 'TS2301'
stderr 'TS2844'
stderr 'TS2663'
! exists initializerReferencingConstructorParameters.js
! exists initializerReferencingConstructorParameters.d.ts
! exists initializerReferencingConstructorParameters.js.map

-- initializerReferencingConstructorParameters.ts --
// Initializer expressions for instance member variables are evaluated in the scope of the class constructor body but are not permitted to reference parameters or local variables of the constructor. 

class C {
    a = x; // error
    b: typeof x; // error
    constructor(x) { }
}

class D {
    a = x; // error
    b: typeof x; // error
    constructor(public x) { }
}

class E {
    a = this.x; // ok
    b: typeof this.x; // ok
    constructor(public x) { }
}

class F<T> {
    a = this.x; // ok
    b = x; // error
    constructor(public x: T) { }
}
