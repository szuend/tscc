# Ported from tests/cases/conformance/types/stringLiteral/stringLiteralCheckedInIf02.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js stringLiteralCheckedInIf02.js stringLiteralCheckedInIf02.ts
! stderr .
cmp stringLiteralCheckedInIf02.js stringLiteralCheckedInIf02.js.golden
! exists stringLiteralCheckedInIf02.d.ts
! exists stringLiteralCheckedInIf02.js.map

-- stringLiteralCheckedInIf02.ts --
type S = "a" | "b";
type T = S[] | S;

function isS(t: T): t is S {
    return t === "a" || t === "b";
}

function f(foo: T) {
    if (isS(foo)) {
        return foo;
    }
    else { 
        return foo[0];
    }
}
-- stringLiteralCheckedInIf02.js.golden --
"use strict";
function isS(t) {
    return t === "a" || t === "b";
}
function f(foo) {
    if (isS(foo)) {
        return foo;
    }
    else {
        return foo[0];
    }
}
