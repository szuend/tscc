# Strict mode makes `var arguments = []` a compile error.
# tscc targets build systems, so no JS is emitted on error.
! exec tscc --target es2015 --out-js a.js a.ts
stderr 'TS1100'
! exists a.js

-- a.ts --
function f() {
    var arguments = [];
}