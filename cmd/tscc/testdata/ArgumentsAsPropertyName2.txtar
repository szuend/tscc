# Ported from tests/cases/compiler/argumentsAsPropertyName2.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --no-strict --target es2015 --out-js argumentsAsPropertyName2.js argumentsAsPropertyName2.ts
! stderr .
cmp argumentsAsPropertyName2.js argumentsAsPropertyName2.js.golden

-- argumentsAsPropertyName2.ts --
// target: es5

function foo() {
    for (let x = 0; x < 1; ++x) {
        let i : number;
        [].forEach(function () { i });
        ({ arguments: 0 });
        ({ arguments });
        ({ arguments: arguments });
    }
}
-- argumentsAsPropertyName2.js.golden --
"use strict";
// target: es5
function foo() {
    for (let x = 0; x < 1; ++x) {
        let i;
        [].forEach(function () { i; });
        ({ arguments: 0 });
        ({ arguments });
        ({ arguments: arguments });
    }
}
