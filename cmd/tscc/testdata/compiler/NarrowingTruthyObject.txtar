# Ported from tests/cases/compiler/narrowingTruthyObject.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --strict --target es2015 --lib es2015,dom --out-js narrowingTruthyObject.js narrowingTruthyObject.ts
stderr 'TS18047'
! exists narrowingTruthyObject.js
! exists narrowingTruthyObject.d.ts
! exists narrowingTruthyObject.js.map

-- narrowingTruthyObject.ts --
function foo(x: unknown, b: boolean) {
    if (typeof x === 'object') {
        x.toString();
    }
    if (typeof x === 'object' && x) {
        x.toString();
    }
    if (x && typeof x === 'object') {
        x.toString();
    }
    if (b && x && typeof x === 'object') {
        x.toString();
    }
    if (x && b && typeof x === 'object') {
        x.toString();
    }
    if (x && b && b && typeof x === 'object') {
        x.toString();
    }
    if (b && b && x && b && b && typeof x === 'object') {
        x.toString();
    }
}

// Repro from #36870

function f1(x: unknown): any {
    return x && typeof x === 'object' && x.hasOwnProperty('x');
}
