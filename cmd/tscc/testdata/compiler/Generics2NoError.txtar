# Ported from tests/cases/compiler/generics2NoError.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --out-dts generics2NoError.d.ts --target es2015 --lib es2015,dom --out-js generics2NoError.js generics2NoError.ts
! stderr .
cmp generics2NoError.d.ts generics2NoError.d.ts.golden
cmp generics2NoError.js generics2NoError.js.golden
! exists generics2NoError.js.map

-- generics2NoError.ts --
interface A { a: string; }
interface B extends A { b: string; }
interface C extends B { c: string; }
interface G<T, U extends B> {
    x: T;
    y: U;
}


var v1: {
    x: { a: string; }
    y: { a: string; b: string; c: string };
}; // Ok


var v2: G<{ a: string }, C>;   // Ok, equivalent to G<A, C>
var v4: G<G<A, B>, C>;         // Ok
-- generics2NoError.d.ts.golden --
interface A {
    a: string;
}
interface B extends A {
    b: string;
}
interface C extends B {
    c: string;
}
interface G<T, U extends B> {
    x: T;
    y: U;
}
declare var v1: {
    x: {
        a: string;
    };
    y: {
        a: string;
        b: string;
        c: string;
    };
};
declare var v2: G<{
    a: string;
}, C>;
declare var v4: G<G<A, B>, C>;
-- generics2NoError.js.golden --
"use strict";
var v1; // Ok
var v2; // Ok, equivalent to G<A, C>
var v4; // Ok
