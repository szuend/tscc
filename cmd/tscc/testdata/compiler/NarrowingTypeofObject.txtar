# Ported from tests/cases/compiler/narrowingTypeofObject.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --strict --target es2015 --lib es2015,dom --out-js narrowingTypeofObject.js narrowingTypeofObject.ts
! stderr .
cmp narrowingTypeofObject.js narrowingTypeofObject.js.golden
! exists narrowingTypeofObject.d.ts
! exists narrowingTypeofObject.js.map

-- narrowingTypeofObject.ts --
interface F { (): string }

function test(x: number & { _foo: string }) {
    if (typeof x === 'object') {
        x; 
    }
}

function f1(x: F & { foo: number }) {
    if (typeof x !== "object") {
        x;
    }
}
-- narrowingTypeofObject.js.golden --
"use strict";
function test(x) {
    if (typeof x === 'object') {
        x;
    }
}
function f1(x) {
    if (typeof x !== "object") {
        x;
    }
}
