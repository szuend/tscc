# Ported from tests/cases/compiler/indexSignaturesInferentialTyping.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js indexSignaturesInferentialTyping.js indexSignaturesInferentialTyping.ts
stderr 'TS2322'
! exists indexSignaturesInferentialTyping.js
! exists indexSignaturesInferentialTyping.d.ts
! exists indexSignaturesInferentialTyping.js.map

-- indexSignaturesInferentialTyping.ts --
function foo<T>(items: { [index: number]: T }): T { return undefined; }
function bar<T>(items: { [index: string]: T }): T { return undefined; }

var x1 = foo({ 0: 0, 1: 1 });       // type should be number
var x2 = bar({ 0: 0, 1: 1 });
var x3 = bar({ zero: 0, one: 1 });  // type should be number
