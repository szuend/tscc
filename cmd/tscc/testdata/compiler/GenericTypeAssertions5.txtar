# Ported from tests/cases/compiler/genericTypeAssertions5.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js genericTypeAssertions5.js genericTypeAssertions5.ts
stderr 'TS2322'
stderr 'TS2352'
! exists genericTypeAssertions5.js
! exists genericTypeAssertions5.d.ts
! exists genericTypeAssertions5.js.map

-- genericTypeAssertions5.ts --
interface A {
    foo(): string;
}

interface B extends A {
    bar(): number;
}

interface C extends A {
    baz(): number;
}

var a: A;
var b: B;
var c: C;

function foo2<T extends A>(x: T) {
    var y = x;
    y = a; // error: cannot convert A to T
    y = b; // error: cannot convert B to T
    y = c; // error: cannot convert C to T
    y = <T>a;
    y = <T>b; // error: cannot convert B to T
    y = <T>c; // error: cannot convert C to T
}
