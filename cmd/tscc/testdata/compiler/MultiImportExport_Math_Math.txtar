# Ported from tests/cases/compiler/multiImportExport.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --report-all-diagnostics --module commonjs --target es2015 --lib es2015,dom --out-dts Math.d.ts --out-js Math.js Math/Math.ts
! stderr .
cmp Math.d.ts Math.d.ts.golden
cmp Math.js Math.js.golden
! exists Math/Math.js.map

-- Drawing.ts --
export import Math = require('./Math/Math')
-- Math/Adder.ts --
class Adder {
    add(a: number, b: number) {
        
    }
}

export = Adder;
-- Math/Math.ts --
import Adder = require('./Adder');

var Math = {
    Adder:Adder
};

export = Math
-- consumer.ts --
import Drawing = require('./Drawing');
var addr = new Drawing.Math.Adder();
-- Math.d.ts.golden --
import Adder = require('./Adder');
declare var Math: {
    Adder: typeof Adder;
};
export = Math;
-- Math.js.golden --
"use strict";
const Adder = require("./Adder");
var Math = {
    Adder: Adder
};
module.exports = Math;
