# Ported from tests/cases/compiler/extractInferenceImprovement.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es6 --lib es6,dom --out-js extractInferenceImprovement.js extractInferenceImprovement.ts
stderr 'TS2345'
stderr 'TS2322'
! exists extractInferenceImprovement.js
! exists extractInferenceImprovement.d.ts
! exists extractInferenceImprovement.js.map

-- extractInferenceImprovement.ts --
// repro mostly from https://github.com/Microsoft/TypeScript/issues/25065
function getProperty2<T, K extends keyof T>(obj: T, key: Extract<K, string>): T[K] {
    return obj[key];
}

function getProperty3<T, K extends Extract<keyof T, string>>(obj: T, key: K): T[K] {
    return obj[key];
}

const s = Symbol();
interface StrNum {
    first: string;
    second: number;
    [s]: string;
}
const obj: StrNum = {} as any;

let prop: string;

// should work
prop = getProperty2(obj, 'first');

prop = getProperty3(obj, 'first');

// Should fail
prop = getProperty2(obj, s);

prop = getProperty3(obj, s);
