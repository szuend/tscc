# Ported from tests/cases/compiler/internalAliasFunction.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --out-dts internalAliasFunction.d.ts --target es2015 --lib es2015,dom --out-js internalAliasFunction.js internalAliasFunction.ts
! stderr .
cmp internalAliasFunction.d.ts internalAliasFunction.d.ts.golden
cmp internalAliasFunction.js internalAliasFunction.js.golden
! exists internalAliasFunction.js.map

-- internalAliasFunction.ts --
namespace a {
    export function foo(x: number) {
        return x;
    }
}

namespace c {
    import b = a.foo;
    export var bVal = b(10);
    export var bVal2 = b;
}
-- internalAliasFunction.d.ts.golden --
declare namespace a {
    function foo(x: number): number;
}
declare namespace c {
    import b = a.foo;
    var bVal: number;
    var bVal2: typeof b;
}
-- internalAliasFunction.js.golden --
"use strict";
var a;
(function (a) {
    function foo(x) {
        return x;
    }
    a.foo = foo;
})(a || (a = {}));
var c;
(function (c) {
    var b = a.foo;
    c.bVal = b(10);
    c.bVal2 = b;
})(c || (c = {}));
