# Ported from tests/cases/compiler/homomorphicMappedTypeNesting.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --strict --target es2015 --lib es2015,dom homomorphicMappedTypeNesting.ts
! stderr .
! exists homomorphicMappedTypeNesting.d.ts
! exists homomorphicMappedTypeNesting.js.map

-- homomorphicMappedTypeNesting.ts --
// Repro from #58060

type Box<T extends string> = { v: T };

type Test<T extends string[]> = T

type UnboxArray<T> = {
    [K in keyof T]: T[K] extends Box<infer R> ? R : never;
};

type Identity<T> = { [K in keyof T]: T[K] };

declare function fnBad<T extends Array<Box<string>>>(...args: T): Test<Identity<UnboxArray<T>>>;
