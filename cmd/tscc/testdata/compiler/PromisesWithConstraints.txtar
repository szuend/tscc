# Ported from tests/cases/compiler/promisesWithConstraints.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js promisesWithConstraints.js promisesWithConstraints.ts
stderr 'TS2322'
! exists promisesWithConstraints.js
! exists promisesWithConstraints.d.ts
! exists promisesWithConstraints.js.map

-- promisesWithConstraints.ts --
interface Promise<T> {
    then<U>(cb: (x: T) => Promise<U>): Promise<U>;
}

interface CPromise<T extends { x: any; }> {
    then<U extends { x: any; }>(cb: (x: T) => Promise<U>): Promise<U>;
}

interface Foo { x: any; }
interface Bar { x: any; y: any; }

var a: Promise<Foo>;
declare var b: Promise<Bar>;
a = b; // ok
b = a; // ok

var a2: CPromise<Foo>;
declare var b2: CPromise<Bar>;
a2 = b2; // ok
b2 = a2; // was error
