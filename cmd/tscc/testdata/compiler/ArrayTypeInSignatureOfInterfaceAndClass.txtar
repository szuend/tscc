# Ported from tests/cases/compiler/arrayTypeInSignatureOfInterfaceAndClass.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js arrayTypeInSignatureOfInterfaceAndClass.js arrayTypeInSignatureOfInterfaceAndClass.ts
! stderr .
cmp arrayTypeInSignatureOfInterfaceAndClass.js arrayTypeInSignatureOfInterfaceAndClass.js.golden
! exists arrayTypeInSignatureOfInterfaceAndClass.d.ts
! exists arrayTypeInSignatureOfInterfaceAndClass.js.map

-- arrayTypeInSignatureOfInterfaceAndClass.ts --
declare namespace WinJS {
    class Promise<T> {
        then<U>(success?: (value: T) => Promise<U>, error?: (error: any) => Promise<U>, progress?: (progress: any) => void): Promise<U>;
    }
}
declare namespace Data {
    export interface IListItem<T> {
        itemIndex: number;
        key: any;
        data: T;
        group: any;
        isHeader: boolean;
        cached: boolean;
        isNonSourceData: boolean;
        preventAugmentation: boolean;
    }
    export interface IVirtualList<T> {
        //removeIndices: WinJS.Promise<IListItem<T>[]>;
        removeIndices(indices: number[], options?: any): WinJS.Promise<IListItem<T>[]>;
    }
    export class VirtualList<T> implements IVirtualList<T> {
        //removeIndices: WinJS.Promise<IListItem<T>[]>;
        public removeIndices(indices: number[], options?: any): WinJS.Promise<IListItem<T>[]>;
    }
}
-- arrayTypeInSignatureOfInterfaceAndClass.js.golden --
"use strict";
