# Ported from tests/cases/compiler/parenthesizedJSDocCastAtReturnStatement.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --allow-js --check-js --lib esnext --strict --target es2015 index.js
! stderr .
! exists index.d.ts
! exists index.js.map

-- index.js --
/** @type {Map<string, string | Set<string>>} */
const cache = new Map()

/**
 * @param {string} key
 * @returns {() => string}
 */
const getStringGetter = (key) => {
  return () => {
    return /** @type {string} */ (cache.get(key))
  }
}
