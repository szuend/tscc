# Ported from tests/cases/compiler/coAndContraVariantInferences5.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --strict --target es2015 --lib es2015,dom coAndContraVariantInferences5.ts
stderr 'TS2322'
! exists coAndContraVariantInferences5.d.ts
! exists coAndContraVariantInferences5.js.map

-- coAndContraVariantInferences5.ts --
type Thing = 'a' | 'b';

function f(
    options: SelectOptions<Thing>,
    onChange: (status: Thing | null) => void,
): void {
    select({
        options,
        onChange,
    });
}

declare function select<KeyT extends string>(props: SelectProps<KeyT>): void;

type SelectProps<KeyT extends string> = {
    options?: SelectOptions<KeyT>;
    onChange: (key: KeyT) => void;
};

type SelectOptions<KeyT extends string> =
    | Array<{key: KeyT}>
    | Array<KeyT>;
