# Ported from tests/cases/compiler/promiseIdentityWithConstraints.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js promiseIdentityWithConstraints.js promiseIdentityWithConstraints.ts
stderr 'TS2403'
! exists promiseIdentityWithConstraints.js
! exists promiseIdentityWithConstraints.d.ts
! exists promiseIdentityWithConstraints.js.map

-- promiseIdentityWithConstraints.ts --
export interface IPromise<T, V> {
    then<U extends T, W extends V>(callback: (x: T) => IPromise<U, W>): IPromise<U, W>;
}
export interface Promise<T, V> {
    then<U extends T, W extends V>(callback: (x: T) => Promise<U, W>): Promise<U, W>;
}

// Error because constraint V doesn't match
var x: IPromise<string, number>;
var x: Promise<string, boolean>;
