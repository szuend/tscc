# Ported from tests/cases/compiler/extendArray.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js extendArray.js extendArray.ts
stderr 'TS2552'
! exists extendArray.js
! exists extendArray.d.ts
! exists extendArray.js.map

-- extendArray.ts --
var a = [1,2];
a.forEach(function (v,i,a) {});


declare namespace _Core {
  interface Array {
    collect(fn:(e:_element) => _element[]) : any[];
  }
}


var arr = (<any>Array).prototype;
arr.collect = function (fn) {
    var res = [];
    for (var i = 0; i < this.length; ++i) {
        var tmp = fn(this[i]);
        for (var j = 0; j < tmp.length; ++j) {
            res.push(tmp[j]);
        }
    }
    return res;
};
