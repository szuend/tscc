# Ported from tests/cases/compiler/arrayEvery.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --out-js arrayEvery.js arrayEvery.ts
! stderr .
cmp arrayEvery.js arrayEvery.js.golden
! exists arrayEvery.d.ts
! exists arrayEvery.js.map

-- arrayEvery.ts --
const foo: (number | string)[] = ['aaa'];

const isString = (x: unknown): x is string => typeof x === 'string';

if (foo.every(isString)) {
  foo[0].slice(0);
}
-- arrayEvery.js.golden --
"use strict";
const foo = ['aaa'];
const isString = (x) => typeof x === 'string';
if (foo.every(isString)) {
    foo[0].slice(0);
}
