# Ported from tests/cases/compiler/staticInheritance.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js staticInheritance.js staticInheritance.ts
! stderr .
cmp staticInheritance.js staticInheritance.js.golden
! exists staticInheritance.d.ts
! exists staticInheritance.js.map

-- staticInheritance.ts --
function doThing(x: { n: string }) { }
class A {
    static n: string;
    p = doThing(A); // OK
}
class B extends A {
    p1 = doThing(A); // OK
    p2 = doThing(B); // OK
}
doThing(B); //OK
-- staticInheritance.js.golden --
"use strict";
function doThing(x) { }
class A {
    constructor() {
        this.p = doThing(A); // OK
    }
}
class B extends A {
    constructor() {
        super(...arguments);
        this.p1 = doThing(A); // OK
        this.p2 = doThing(B); // OK
    }
}
doThing(B); //OK
