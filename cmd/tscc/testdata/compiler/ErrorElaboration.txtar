# Ported from tests/cases/compiler/errorElaboration.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js errorElaboration.js errorElaboration.ts
stderr 'TS2300'
stderr 'TS2345'
stderr 'TS2322'
stderr 'TS2538'
stderr 'TS2339'
! exists errorElaboration.js
! exists errorElaboration.d.ts
! exists errorElaboration.js.map

-- errorElaboration.ts --
// Repro for #5712

interface Ref<T> {
    prop: T;
}
interface Container<T> {
    m1: Container<Ref<T>>;
    m2: T;
}
declare function foo(x: () => Container<Ref<number>>): void;
declare let a: () => Container<Ref<string>>;
foo(a);

// Repro for #25498

function test(): {[A in "foo"]: A} {
  return {foo: "bar"};
}

// Repro for #32358

const foo = { bar: 'a' };
const x = ({ [foo.bar]: c }) => undefined;
