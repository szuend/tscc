# Ported from tests/cases/compiler/overloadCallTest.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --no-strict --target es2015 --lib es2015,dom --out-js overloadCallTest.js overloadCallTest.ts
! stderr .
cmp overloadCallTest.js overloadCallTest.js.golden
! exists overloadCallTest.d.ts
! exists overloadCallTest.js.map

-- overloadCallTest.ts --
class foo {
    constructor() {
        function bar(): string;
        function bar(s:string);
        function bar(foo?: string) { return "foo" };

        var test = bar("test");
        var goo = bar();

        goo = bar("test");
    }
 
}

-- overloadCallTest.js.golden --
"use strict";
class foo {
    constructor() {
        function bar(foo) { return "foo"; }
        ;
        var test = bar("test");
        var goo = bar();
        goo = bar("test");
    }
}
