# Ported from tests/cases/compiler/returnConditionalExpressionJSDocCast.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --check-js --strict --target esnext --lib esnext,dom file.js
! stderr .
! exists file.d.ts
! exists file.js.map

-- file.js --
// Don't peek into conditional return expression if it's wrapped in a cast
/** @type {Map<string, string>} */
const sources = new Map();
/**

 * @param {string=} type the type of source that should be generated
 * @returns {String}
 */
function source(type = "javascript") {
    return /** @type {String} */ (
        type
            ? sources.get(type)
            : sources.get("some other thing")
    );
}
