# Ported from tests/cases/compiler/unwitnessedTypeParameterVariance.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --strict --target es2015 --lib es2015,dom --out-js unwitnessedTypeParameterVariance.js unwitnessedTypeParameterVariance.ts
! stderr .
cmp unwitnessedTypeParameterVariance.js unwitnessedTypeParameterVariance.js.golden
! exists unwitnessedTypeParameterVariance.d.ts
! exists unwitnessedTypeParameterVariance.js.map

-- unwitnessedTypeParameterVariance.ts --
// Repros from #33872

export interface CalcObj<O> {
    read: (origin: O) => CalcValue<O>;
}

export type CalcValue<O> = CalcObj<O>;

function foo<O>() {
    const unk: CalcObj<unknown> = { read: (origin: unknown) => unk }
    const x: CalcObj<O> = unk;
}

type A<T> = B<T>;

interface B<T> {
    prop: A<T>;
}

declare let a: A<number>;
declare let b: A<3>;
 
b = a;
-- unwitnessedTypeParameterVariance.js.golden --
// Repros from #33872
function foo() {
    const unk = { read: (origin) => unk };
    const x = unk;
}
b = a;
export {};
