# Ported from tests/cases/compiler/unknownSymbols2.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js unknownSymbols2.js unknownSymbols2.ts
stderr 'TS2304'
stderr 'TS2503'
! exists unknownSymbols2.js
! exists unknownSymbols2.d.ts
! exists unknownSymbols2.js.map

-- unknownSymbols2.ts --
namespace M {
    var x: asdf;
    var y = x + asdf;
    var z = <asdf>x; // should be an error
    if (asdf) {
    }
    else if (qwerty) {
    }

    try {
    }
    catch (asdf) { // no error
    }

    switch (asdf) {
        case qwerty:
            break;
        default:
            break;
    }

    var a = () => asdf;
    var b = (asdf) => { return qwerty };

    namespace N {
        var x = 1;
    }
    import c = N;
    import d = asdf;
}
