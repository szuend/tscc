# Ported from tests/cases/compiler/selfReferencingTypeReferenceInference.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js selfReferencingTypeReferenceInference.js selfReferencingTypeReferenceInference.ts
! stderr .
cmp selfReferencingTypeReferenceInference.js selfReferencingTypeReferenceInference.js.golden
! exists selfReferencingTypeReferenceInference.d.ts
! exists selfReferencingTypeReferenceInference.js.map

-- selfReferencingTypeReferenceInference.ts --
interface Box<T> {
    __: T
}

type Recursive<T> =
    | T
    | Box<Recursive<T>>

type InferRecursive<T> = T extends Recursive<infer R> ? R : "never!"

// the type we are testing with
type t1 = Box<string | Box<number | boolean>>

type t2 = InferRecursive<t1>
type t3 = InferRecursive<Box<string | Box<number | boolean>>> // write t1 explicitly

  // Why is t2 and t3 different??
  // They have same input type!
-- selfReferencingTypeReferenceInference.js.golden --
"use strict";
// Why is t2 and t3 different??
// They have same input type!
