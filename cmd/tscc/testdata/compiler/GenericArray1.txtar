# Ported from tests/cases/compiler/genericArray1.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --out-dts genericArray1.d.ts --target es2015 --lib es2015,dom --out-js genericArray1.js genericArray1.ts
! stderr .
cmp genericArray1.d.ts genericArray1.d.ts.golden
cmp genericArray1.js genericArray1.js.golden
! exists genericArray1.js.map

-- genericArray1.ts --
/*
var n: number[];

interface Array<T> {
map<U>(callbackfn: (value: T, index: number, array: T[]) => U, thisArg?: any): U[];
}

interface String{
	length: number;
}
*/

var lengths = ["a", "b", "c"].map(x => x.length);
-- genericArray1.d.ts.golden --
declare var lengths: number[];
-- genericArray1.js.golden --
"use strict";
/*
var n: number[];

interface Array<T> {
map<U>(callbackfn: (value: T, index: number, array: T[]) => U, thisArg?: any): U[];
}

interface String{
    length: number;
}
*/
var lengths = ["a", "b", "c"].map(x => x.length);
