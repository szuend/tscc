# Ported from tests/cases/compiler/argumentsReferenceInFunction1_Js.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --check-js --strict --target es2015 --lib es2015,dom index.js
stderr 'TS7006'
stderr 'TS2345'
! exists index.d.ts
! exists index.js.map

-- index.js --
const format = function(f) {
  var str = '';
  var i = 1;
  var args = arguments;
  var len = args.length;
  for (var x = args[i]; i < len; x = args[++i]) {
    str += ' ' + x;
  }
  return str;
};

const debuglog = function() {
  return format.apply(null, arguments);
};
