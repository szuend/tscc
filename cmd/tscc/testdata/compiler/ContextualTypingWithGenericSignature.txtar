# Ported from tests/cases/compiler/contextualTypingWithGenericSignature.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js contextualTypingWithGenericSignature.js contextualTypingWithGenericSignature.ts
! stderr .
cmp contextualTypingWithGenericSignature.js contextualTypingWithGenericSignature.js.golden
! exists contextualTypingWithGenericSignature.d.ts
! exists contextualTypingWithGenericSignature.js.map

-- contextualTypingWithGenericSignature.ts --
// If e is a FunctionExpression or ArrowFunctionExpression with no type parameters and no parameter or return type annotations, and T is a function type with EXACTLY ONE non - generic call signature, then any inferences made for type parameters referenced by the parameters of T’s call signature are fixed(section 4.12.2) and e is processed with the contextual type T, as described in section 4.9.3.

var f2: {
    <T, U>(x: T, y: U): T
};

f2 = (x, y) => { return x }
-- contextualTypingWithGenericSignature.js.golden --
"use strict";
// If e is a FunctionExpression or ArrowFunctionExpression with no type parameters and no parameter or return type annotations, and T is a function type with EXACTLY ONE non - generic call signature, then any inferences made for type parameters referenced by the parameters of T’s call signature are fixed(section 4.12.2) and e is processed with the contextual type T, as described in section 4.9.3.
var f2;
f2 = (x, y) => { return x; };
