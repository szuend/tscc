# Ported from tests/cases/compiler/genericTupleWithSimplifiableElements.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --strict --target es2015 --lib es2015,dom genericTupleWithSimplifiableElements.ts
! stderr .
! exists genericTupleWithSimplifiableElements.d.ts
! exists genericTupleWithSimplifiableElements.js.map

-- genericTupleWithSimplifiableElements.ts --
// repro from #52354

type SS1 = string;
let y: [t: "AAA", ...args: { [S in SS1]: [a: number]; }[SS1]] = ["AAA", 1];

type SS2 = "1" | "2" | "3";
let z: [t: "AAA", ...args: { [S in SS2]: [a: number]; }[SS2]] = ["AAA", 1];

class I<SS extends string>{
    f() {
        let w: [...args: { [S in SS]: [a: number]; }[SS]] = [1];

        let x: [t: "AAA", ...args: { [S in SS]: [a: number]; }[SS]] = ["AAA", 1];
    }
}
