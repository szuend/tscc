# Ported from tests/cases/compiler/typeParameterLeak.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --strict --target es2015 --lib es2015,dom --out-js typeParameterLeak.js typeParameterLeak.ts
! stderr .
cmp typeParameterLeak.js typeParameterLeak.js.golden
! exists typeParameterLeak.d.ts
! exists typeParameterLeak.js.map

-- typeParameterLeak.ts --
// Repro from #35655

interface Box<T> { data: T }
type BoxTypes = Box<{ x: string }> | Box<{ y: string }>;

type BoxFactoryFactory<TBox> = TBox extends Box<infer T> ? {
  (arg: T): BoxFactory<TBox> | undefined
} : never;

interface BoxFactory<A> {
  getBox(): A,
}

declare const f: BoxFactoryFactory<BoxTypes>;
const b = f({ x: "", y: "" })?.getBox();
if (b) {
  const x = b.data;
}
-- typeParameterLeak.js.golden --
"use strict";
// Repro from #35655
var _a;
const b = (_a = f({ x: "", y: "" })) === null || _a === void 0 ? void 0 : _a.getBox();
if (b) {
    const x = b.data;
}
