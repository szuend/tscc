# Ported from tests/cases/compiler/knockout.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js knockout.js knockout.ts
stderr 'TS2339'
! exists knockout.js
! exists knockout.d.ts
! exists knockout.js.map

-- knockout.ts --
  declare namespace ko {
   export interface Observable<T> {
     (): T;
     (value: T): any;
     N: number;
     g: boolean;
     r: T;
   }
   export function observable<T>(value: T): Observable<T>;
 }
 var o = {
     name: ko.observable("Bob"),
     age: ko.observable(37)
 }
 var x_v = o.name().length
 var age_v = o.age();
 var name_v = o.name("Robert");
 var zz_v = o.name.N;
 var yy_v = o.name.g;
 var rr_v = o.name.r;
 var dd_v = o.name.d;
