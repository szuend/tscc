# Ported from tests/cases/compiler/genericMethodOverspecialization.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js genericMethodOverspecialization.js genericMethodOverspecialization.ts
! stderr .
cmp genericMethodOverspecialization.js genericMethodOverspecialization.js.golden
! exists genericMethodOverspecialization.d.ts
! exists genericMethodOverspecialization.js.map

-- genericMethodOverspecialization.ts --
var names = ["list", "table1", "table2", "table3", "summary"];

interface HTMLElement {
    clientWidth: number;
    isDisabled: boolean;
}

declare var document: Document;
interface Document {
    getElementById(elementId: string): HTMLElement;
}

var elements = names.map(function (name) {
    return document.getElementById(name);
});


var xxx = elements.filter(function (e) {
    return !e.isDisabled;
});

var widths:number[] = elements.map(function (e) { // should not error
    return e.clientWidth;
});

-- genericMethodOverspecialization.js.golden --
"use strict";
var names = ["list", "table1", "table2", "table3", "summary"];
var elements = names.map(function (name) {
    return document.getElementById(name);
});
var xxx = elements.filter(function (e) {
    return !e.isDisabled;
});
var widths = elements.map(function (e) {
    return e.clientWidth;
});
