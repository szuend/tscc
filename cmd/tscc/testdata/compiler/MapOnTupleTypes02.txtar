# Ported from tests/cases/compiler/mapOnTupleTypes02.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --out-dts mapOnTupleTypes02.d.ts --no-implicit-any --strict-null-checks --target es2015 --lib es2015,dom --out-js mapOnTupleTypes02.js mapOnTupleTypes02.ts
! stderr .
cmp mapOnTupleTypes02.d.ts mapOnTupleTypes02.d.ts.golden
cmp mapOnTupleTypes02.js mapOnTupleTypes02.js.golden
! exists mapOnTupleTypes02.js.map

-- mapOnTupleTypes02.ts --
export type Point = [number, number];

export function increment(point: Point) {
  return point.map(d => d + 1);
}
-- mapOnTupleTypes02.d.ts.golden --
export type Point = [number, number];
export declare function increment(point: Point): number[];
-- mapOnTupleTypes02.js.golden --
export function increment(point) {
    return point.map(d => d + 1);
}
