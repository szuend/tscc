# Ported from tests/cases/compiler/recursiveIdenticalOverloadResolution.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js recursiveIdenticalOverloadResolution.js recursiveIdenticalOverloadResolution.ts
stderr 'TS2454'
! exists recursiveIdenticalOverloadResolution.js
! exists recursiveIdenticalOverloadResolution.d.ts
! exists recursiveIdenticalOverloadResolution.js.map

-- recursiveIdenticalOverloadResolution.ts --
namespace M {

   interface I { (i: I): I; }

   function f(p: I) { return f };

   var i: I;

   f(i);

   f(f(i));

   f((f(f(i))));

}
