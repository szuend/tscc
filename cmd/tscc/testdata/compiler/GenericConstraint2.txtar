# Ported from tests/cases/compiler/genericConstraint2.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js genericConstraint2.js genericConstraint2.ts
stderr 'TS2420'
stderr 'TS2741'
! exists genericConstraint2.js
! exists genericConstraint2.d.ts
! exists genericConstraint2.js.map

-- genericConstraint2.ts --
interface Comparable<T> {
    comparer(other: T): number;
}

function compare<T extends Comparable<T>>(x: T, y: T): number {
    if (x == null) return y == null ? 0 : -1;
    if (y == null) return 1;
    return x.comparer(y);
}

class ComparableString implements Comparable<string>{
    constructor(public currentValue: string) { }

    localeCompare(other) {
        return 0;
    }
}

var a = new ComparableString("a");
var b = new ComparableString("b");
var c = compare<ComparableString>(a, b);
