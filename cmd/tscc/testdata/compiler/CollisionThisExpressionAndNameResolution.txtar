# Ported from tests/cases/compiler/collisionThisExpressionAndNameResolution.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --lib es5 --no-strict --target es2015 --out-js collisionThisExpressionAndNameResolution.js collisionThisExpressionAndNameResolution.ts
! stderr .
cmp collisionThisExpressionAndNameResolution.js collisionThisExpressionAndNameResolution.js.golden
! exists collisionThisExpressionAndNameResolution.d.ts
! exists collisionThisExpressionAndNameResolution.js.map

-- collisionThisExpressionAndNameResolution.ts --
var console : {
    log(message: any);
}
class Foo {
    x() {
        var _this = 10; // Local var. No this capture in x(), so no conflict.
        function inner() {
            console.log(_this); // Error as this doesnt not resolve to user defined _this
            return x => this;   // New scope.  So should inject new _this capture into function inner
        }
    }
}
-- collisionThisExpressionAndNameResolution.js.golden --
"use strict";
var console;
class Foo {
    x() {
        var _this = 10; // Local var. No this capture in x(), so no conflict.
        function inner() {
            console.log(_this); // Error as this doesnt not resolve to user defined _this
            return x => this; // New scope.  So should inject new _this capture into function inner
        }
    }
}
