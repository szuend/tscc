# Ported from tests/cases/compiler/commentOnAmbientModule.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --no-strict --target es2015 --lib es2015,dom --out-js a.js a.ts
! stderr .
cmp a.js a.js.golden
! exists a.d.ts
! exists a.js.map

-- a.ts --
/*!=========
    Keep this pinned comment
   =========
*/

/*! Don't keep this pinned comment */
declare namespace C {
    function foo();
}

// Don't keep this comment.
declare namespace D {
    class bar { }
}
-- b.ts --
///<reference path="a.ts"/>
declare namespace E {
    class foobar extends D.bar {
        foo();
    }
}
-- a.js.golden --
"use strict";
/*!=========
    Keep this pinned comment
   =========
*/
