# Ported from tests/cases/compiler/targetTypeCastTest.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js targetTypeCastTest.js targetTypeCastTest.ts
stderr 'TS2300'
! exists targetTypeCastTest.js
! exists targetTypeCastTest.d.ts
! exists targetTypeCastTest.js.map

-- targetTypeCastTest.ts --
declare var Point: { new(x:number, y:number): {x: number; y: number; }; }

function Point(x, y) {
    this.x = x;
    this.y = y;   
}

interface Adder {
    (x: number, y: number): number;   
}

var add = <Adder>function(x,y) {    return x+ y;   }


interface Adder2 {
    (x: number, y: number): number;   
}

var add2: Adder2 = function(x,y) {
    return 0;
}

function add3(x,y) {x}
