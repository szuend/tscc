# Ported from tests/cases/compiler/contextualTypeSelfReferencing.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --strict --target es2015 --lib es2015,dom contextualTypeSelfReferencing.ts
! stderr .
! exists contextualTypeSelfReferencing.d.ts
! exists contextualTypeSelfReferencing.js.map

-- contextualTypeSelfReferencing.ts --
// repro from https://github.com/microsoft/TypeScript/issues/54048

type narrow<def> = def extends string
  ? def
  : def extends [unknown, ...unknown[]]
  ? def
  : {
      [k in keyof def]: narrow<def[k]>;
    };

declare const parse: <def>(def: narrow<def>) => def;

const result = parse([{ a: "foo" }]);
