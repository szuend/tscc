# Ported from tests/cases/compiler/constraints0.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js constraints0.js constraints0.ts
stderr 'TS2741'
! exists constraints0.js
! exists constraints0.d.ts
! exists constraints0.js.map

-- constraints0.ts --
interface A {
	a: number;
}

interface B {
	b: string;
}

interface C<T extends A> {
    x: T;
}

declare var v1: C<A>; // should work
declare var v2: C<B>; // should not work

var y = v1.x.a; // 'a' should be of type 'number'
