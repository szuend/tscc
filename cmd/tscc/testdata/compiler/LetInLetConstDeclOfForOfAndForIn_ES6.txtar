# Ported from tests/cases/compiler/letInLetConstDeclOfForOfAndForIn_ES6.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es6 --lib es6,dom --out-js letInLetConstDeclOfForOfAndForIn_ES6.js letInLetConstDeclOfForOfAndForIn_ES6.ts
stderr 'TS1212'
! exists letInLetConstDeclOfForOfAndForIn_ES6.js
! exists letInLetConstDeclOfForOfAndForIn_ES6.d.ts
! exists letInLetConstDeclOfForOfAndForIn_ES6.js.map

-- letInLetConstDeclOfForOfAndForIn_ES6.ts --
// Should be an error
for (let let of [1,2,3]) {}

for (const let of [1,2,3]) {}

for (let let in [1,2,3]) {}

for (const let in [1,2,3]) {}

{
	for (let let of [1,2,3]) {}

	for (const let of [1,2,3]) {}
	
	for (let let in [1,2,3]) {}

	for (const let in [1,2,3]) {}
}

