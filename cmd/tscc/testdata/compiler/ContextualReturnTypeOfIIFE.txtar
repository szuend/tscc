# Ported from tests/cases/compiler/contextualReturnTypeOfIIFE.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --lib esnext --target es2015 --out-js contextualReturnTypeOfIIFE.js contextualReturnTypeOfIIFE.ts
! stderr .
cmp contextualReturnTypeOfIIFE.js contextualReturnTypeOfIIFE.js.golden
! exists contextualReturnTypeOfIIFE.d.ts
! exists contextualReturnTypeOfIIFE.js.map

-- contextualReturnTypeOfIIFE.ts --
const test1: Promise<[one: number, two: string]> = (async () => {
    return [1, 'two'];
})();

const test2: Promise<[one: number, two: string]> = new Promise(
    (resolve) => resolve([1, 'two']),
);

const obj: { foo: [one: number, two: string] } = {
    foo: (() => [1, 'two'])()
};
-- contextualReturnTypeOfIIFE.js.golden --
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
const test1 = (() => __awaiter(void 0, void 0, void 0, function* () {
    return [1, 'two'];
}))();
const test2 = new Promise((resolve) => resolve([1, 'two']));
const obj = {
    foo: (() => [1, 'two'])()
};
