# Ported from tests/cases/compiler/truthinessCallExpressionCoercion3.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --lib esnext,dom --strict-null-checks --target es2015 --out-js truthinessCallExpressionCoercion3.js truthinessCallExpressionCoercion3.ts
! stderr .
cmp truthinessCallExpressionCoercion3.js truthinessCallExpressionCoercion3.js.golden
! exists truthinessCallExpressionCoercion3.d.ts
! exists truthinessCallExpressionCoercion3.js.map

-- truthinessCallExpressionCoercion3.ts --
// from #41640, based on an example in ant-design
interface I {
    always(): void
}

function f(result: unknown) {
    if ((result as I).always) {
        return result
    }
}
function g(result: unknown) {
    if (((result as I)).always) {
        return result
    }
}
-- truthinessCallExpressionCoercion3.js.golden --
"use strict";
function f(result) {
    if (result.always) {
        return result;
    }
}
function g(result) {
    if (result.always) {
        return result;
    }
}
