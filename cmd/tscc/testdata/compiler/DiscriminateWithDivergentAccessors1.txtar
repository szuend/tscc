# Ported from tests/cases/compiler/discriminateWithDivergentAccessors1.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --strict --target es2015 --lib es2015,dom discriminateWithDivergentAccessors1.ts
! stderr .
! exists discriminateWithDivergentAccessors1.d.ts
! exists discriminateWithDivergentAccessors1.js.map

-- discriminateWithDivergentAccessors1.ts --
type WeirdoBox<T> =
  | { get done(): false; set done(v: T | null) }
  | { get done(): true; set done(v: T | null); value: T };

declare const weirdoBox: WeirdoBox<number>;

if (weirdoBox.done) {
  weirdoBox.value;
}

type WeirdoBox2<T> =
  | { get done(): false; set done(v: T | null) }
  | { get done(): true; set done(v: T | null); value: T }
  | { get done(): true; set done(v: T | null | undefined); value: number };

declare const weirdoBox2: WeirdoBox2<string>;

if (weirdoBox2.done) {
  weirdoBox2.value;
}
