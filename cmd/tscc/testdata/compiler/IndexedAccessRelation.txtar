# Ported from tests/cases/compiler/indexedAccessRelation.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js indexedAccessRelation.js indexedAccessRelation.ts
stderr 'TS2322'
! exists indexedAccessRelation.js
! exists indexedAccessRelation.d.ts
! exists indexedAccessRelation.js.map

-- indexedAccessRelation.ts --
// Repro from #14723

class Component<S> {
    setState<K extends keyof S>(state: Pick<S, K>) {}
}

export interface State<T> {
    a?: T;
}

class Foo {}

class Comp<T extends Foo, S> extends Component<S & State<T>>
{
    foo(a: T) {
        this.setState({ a: a });
    }
}
