# Ported from tests/cases/compiler/partialDiscriminatedUnionMemberHasGoodError.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js partialDiscriminatedUnionMemberHasGoodError.js partialDiscriminatedUnionMemberHasGoodError.ts
stderr 'TS2322'
! exists partialDiscriminatedUnionMemberHasGoodError.js
! exists partialDiscriminatedUnionMemberHasGoodError.d.ts
! exists partialDiscriminatedUnionMemberHasGoodError.js.map

-- partialDiscriminatedUnionMemberHasGoodError.ts --
interface TypeA {
    type: "A";
    param: string;
}

interface TypeB {
    type: "B";
    param: string;
}

type ValidType = TypeA | TypeB;

interface Wrapper {
    types: ValidType[];
}

const foo: Wrapper[] = [];

foo.push({
    types: [{
        type: "A"
    }]
});
