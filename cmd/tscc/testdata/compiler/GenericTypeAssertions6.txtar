# Ported from tests/cases/compiler/genericTypeAssertions6.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js genericTypeAssertions6.js genericTypeAssertions6.ts
stderr 'TS2352'
! exists genericTypeAssertions6.js
! exists genericTypeAssertions6.d.ts
! exists genericTypeAssertions6.js.map

-- genericTypeAssertions6.ts --
class A<T,U> {
    constructor(x) {
        var y = <T>x;
        var z = <U>x;
    }

    f(x: T, y: U) {
        x = <T>y;
        y = <U>x;
    }
}

class B<T extends Date, U extends Date> extends A<T, U> {
    g(x: T) {
        var a: Date = x;
        var b = <Date>x;
        var c = <T>new Date();
        var d = <U>new Date();
        var e = <T><U>new Date();
    }
}

declare var b: B<Date, Date>;
var c: A<Date, Date> = <A<Date, Date>>b;
