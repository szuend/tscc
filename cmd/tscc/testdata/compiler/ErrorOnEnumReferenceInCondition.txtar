# Ported from tests/cases/compiler/errorOnEnumReferenceInCondition.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --strict --target es2015 --lib es2015,dom --out-js errorOnEnumReferenceInCondition.js errorOnEnumReferenceInCondition.ts
stderr 'TS2845'
! exists errorOnEnumReferenceInCondition.js
! exists errorOnEnumReferenceInCondition.d.ts
! exists errorOnEnumReferenceInCondition.js.map

-- errorOnEnumReferenceInCondition.ts --
enum Nums {
    Zero = 0,
    One = 1,
}

const a = Nums.Zero ? "a" : "b";
const b = Nums.One ? "a" : "b";

if (Nums.Zero) {
    Nums;
}
else {
    Nums;
}


if (Nums.One) {
    Nums;
}
else {
    Nums;
}


enum Strs {
    Empty = "",
    A = "A",
}

const c = Strs.Empty ? "a" : "b";
const d = Strs.A ? "a" : "b";

if (Strs.Empty) {
    Strs;
}
else {
    Strs;
}


if (Strs.A) {
    Strs;
}
else {
    Strs;
}
