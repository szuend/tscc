# Ported from tests/cases/compiler/anyInferenceAnonymousFunctions.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --out-js anyInferenceAnonymousFunctions.js anyInferenceAnonymousFunctions.ts
stderr 'TS2454'
! exists anyInferenceAnonymousFunctions.js
! exists anyInferenceAnonymousFunctions.d.ts
! exists anyInferenceAnonymousFunctions.js.map

-- anyInferenceAnonymousFunctions.ts --
var paired: any[];

paired.reduce(function (a1, a2) {

    return a1.concat({});

} , []);

paired.reduce((b1, b2) => {

    return b1.concat({});
} , []);

paired.reduce((b3, b4) => b3.concat({}), []);

paired.map((c1) => c1.count);
paired.map(function (c2) { return c2.count; });
