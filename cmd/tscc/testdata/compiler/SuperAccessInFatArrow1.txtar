# Ported from tests/cases/compiler/superAccessInFatArrow1.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js superAccessInFatArrow1.js superAccessInFatArrow1.ts
! stderr .
cmp superAccessInFatArrow1.js superAccessInFatArrow1.js.golden
! exists superAccessInFatArrow1.d.ts
! exists superAccessInFatArrow1.js.map

-- superAccessInFatArrow1.ts --
namespace test {
    export class A {
        foo() {
        }
    }
    export class B extends A {
        bar(callback: () => void ) {
        }
        runme() {
            this.bar(() => {
                super.foo();
            });
        }
    }
}
-- superAccessInFatArrow1.js.golden --
"use strict";
var test;
(function (test) {
    class A {
        foo() {
        }
    }
    test.A = A;
    class B extends A {
        bar(callback) {
        }
        runme() {
            this.bar(() => {
                super.foo();
            });
        }
    }
    test.B = B;
})(test || (test = {}));
