# Ported from tests/cases/compiler/orderMattersForSignatureGroupIdentity.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js orderMattersForSignatureGroupIdentity.js orderMattersForSignatureGroupIdentity.ts
stderr 'TS2769'
stderr 'TS2339'
stderr 'TS2403'
! exists orderMattersForSignatureGroupIdentity.js
! exists orderMattersForSignatureGroupIdentity.d.ts
! exists orderMattersForSignatureGroupIdentity.js.map

-- orderMattersForSignatureGroupIdentity.ts --
interface A {
    (x: { s: string }): string
    (x: { n: number }): number
}

interface B {
    (x: { s: string }): string
    (x: { n: number }): number
}

interface C {
    (x: { n: number }): number
    (x: { s: string }): string
}

declare var v: A;
declare var v: B;

v({ s: "", n: 0 }).toLowerCase();

declare var w: A;
declare var w: C;

w({ s: "", n: 0 }).toLowerCase();
