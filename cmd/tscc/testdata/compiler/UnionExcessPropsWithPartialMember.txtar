# Ported from tests/cases/compiler/unionExcessPropsWithPartialMember.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --strict --target es2015 --lib es2015,dom --out-js unionExcessPropsWithPartialMember.js unionExcessPropsWithPartialMember.ts
! stderr .
cmp unionExcessPropsWithPartialMember.js unionExcessPropsWithPartialMember.js.golden
! exists unionExcessPropsWithPartialMember.d.ts
! exists unionExcessPropsWithPartialMember.js.map

-- unionExcessPropsWithPartialMember.ts --
interface A {
    unused?: string;
    x: string;
}

interface B {
    x: string;
    y: string;
}

declare var ab: A | B;
declare var a: A;

ab = {...a, y: (null as any as string | undefined)}; // Should be allowed, since `y` is missing on `A`
-- unionExcessPropsWithPartialMember.js.golden --
"use strict";
ab = Object.assign(Object.assign({}, a), { y: null }); // Should be allowed, since `y` is missing on `A`
