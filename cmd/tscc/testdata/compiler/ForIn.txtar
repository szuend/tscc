# Ported from tests/cases/compiler/forIn.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --no-strict --target es2015 --lib es2015,dom --out-js forIn.js forIn.ts
stderr 'TS2404'
stderr 'TS2304'
! exists forIn.js
! exists forIn.d.ts
! exists forIn.js.map

-- forIn.ts --
var arr = null;
for (var i:number in arr) { // error
    var x1 = arr[i];
    var y1 = arr[i];
}

for (var j in arr) { // ok
    var x2 = arr[j];
    var y2 = arr[j];
}

var arr2 = [];
for (j in arr2) { // ok
    var x3 = arr2[j];
    var y3 = arr2[j];
}

for (var l in arr) { 
   // error in the body
   k[l] = 1;
}
