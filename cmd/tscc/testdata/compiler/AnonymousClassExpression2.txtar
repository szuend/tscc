# Ported from tests/cases/compiler/anonymousClassExpression2.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js anonymousClassExpression2.js anonymousClassExpression2.ts
stderr 'TS2551'
! exists anonymousClassExpression2.js
! exists anonymousClassExpression2.d.ts
! exists anonymousClassExpression2.js.map

-- anonymousClassExpression2.ts --
// Fixes #14860
// note: repros with `while (0);` too
// but it's less inscrutable and more obvious to put it *inside* the loop
while (0) {
    class A {
        methodA() {
            this; //note: a this reference of some kind is required to trigger the bug
        }
    }

    class B {
        methodB() {
            this.methodA; // error
            this.methodB; // ok
        }
    }
}
