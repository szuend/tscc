# Ported from tests/cases/compiler/moduleAugmentationExtendFileModule2.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --module commonjs --target es2015 --lib es2015,dom --path ./observable=map.ts --out-js observable.js observable.ts
! stderr .
cmp observable.js observable.js.golden
! exists observable.d.ts
! exists observable.js.map

-- main.ts --
import { Observable } from "./observable"
import "./map";

let x: Observable<number>;
let y = x.map(x => x + 1);
let z1 = Observable.someValue.toFixed();
let z2 = Observable.someAnotherValue.toLowerCase();
-- map.ts --
import { Observable } from "./observable"

(<any>Observable.prototype).map = function() { }

declare module "./observable" {
    interface Observable<T> {
        map<U>(proj: (e:T) => U): Observable<U>
    }
    namespace Observable {
        let someAnotherValue: string;
    }
}
-- observable.ts --
export declare class Observable<T> {
    filter(pred: (e:T) => boolean): Observable<T>;
}

export namespace Observable {
    export let someValue: number;
}

-- observable.js.golden --
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Observable = void 0;
var Observable;
(function (Observable) {
})(Observable || (exports.Observable = Observable = {}));
