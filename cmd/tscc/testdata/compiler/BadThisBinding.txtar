# Ported from tests/cases/compiler/badThisBinding.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
exec tscc --target es2015 --lib es2015,dom --out-js badThisBinding.js badThisBinding.ts
! stderr .
cmp badThisBinding.js badThisBinding.js.golden
! exists badThisBinding.d.ts
! exists badThisBinding.js.map

-- badThisBinding.ts --
declare function foo(a:any): any;
declare function bar(a:any): any;

class Greeter {
    constructor() {
		foo(() => {
            bar(() => {
                var x = this;
			});
		});
	}

} 
-- badThisBinding.js.golden --
"use strict";
class Greeter {
    constructor() {
        foo(() => {
            bar(() => {
                var x = this;
            });
        });
    }
}
