# Ported from tests/cases/compiler/typePredicateInLoop.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --target es2015 --lib es2015,dom --out-js typePredicateInLoop.js typePredicateInLoop.ts
stderr 'TS1345'
! exists typePredicateInLoop.js
! exists typePredicateInLoop.d.ts
! exists typePredicateInLoop.js.map

-- typePredicateInLoop.ts --
// Repro from #12101

interface Type {
  type: number;
}

interface TypeExt extends Type {
  arr: Type[];
}

const guard = (arg: Type): arg is TypeExt => arg.type === 1;
const otherFunc = (arg1: Type, arg2: TypeExt): void => {};

export function y(arg: Type): void {
  if (guard(arg)) {
    for (const ITEM of arg.arr) {
      if (otherFunc(ITEM, arg)) {
      }
    }
  }
}
