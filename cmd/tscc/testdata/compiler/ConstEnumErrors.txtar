# Ported from tests/cases/compiler/constEnumErrors.ts by tools/portcase.
# DO NOT EDIT by hand; re-run the porter if the upstream baseline changes.
! exec tscc --lib es5 --target es2015 --out-js constEnumErrors.js constEnumErrors.ts
stderr 'TS2567'
stderr 'TS2651'
stderr 'TS2474'
stderr 'TS2339'
stderr 'TS2476'
stderr 'TS2475'
stderr 'TS2477'
stderr 'TS2478'
! exists constEnumErrors.js
! exists constEnumErrors.d.ts
! exists constEnumErrors.js.map

-- constEnumErrors.ts --
const enum E {
    A
}

namespace E {
    var x = 1;
}

const enum E1 {
    // illegal case
    // forward reference to the element of the same enum
    X = Y,
    // forward reference to the element of the same enum
    Y = E1.Z,
    Y1 = E1["Z"]
}

const enum E2 {
    A
}

var y0 = E2[1]
var name = "A";
var y1 = E2[name];
var y2 = E2[`${name}`];

var x = E2;
var y = [E2];

function foo(t: any): void {
}

foo(E2);

const enum NaNOrInfinity {
    A = 9007199254740992,
    B = A * A,
    C = B * B,
    D = C * C,
    E = D * D,
    F = E * E, // overflow
    G = 1 / 0, // overflow
    H = 0 / 0  // NaN
}
